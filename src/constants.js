export let fontProperties = {
  family: "sans-serif",
  size: "16px",
  margins: {
    top: 10,
    right: 10,
    bottom: 10,
    left: 10
  }
};

export let blockProperties = {
  borderRadius: 8,
  offset: 2
};
